<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Design and Development of a Large Language Model for Recognizing the Usage of Traditional Herbs</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <header>
        <h1>Design and Development of a Large Language Model for Recognizing the Usage of Traditional Herbs</h1>
        <p>Available Files for Download</p>
    </header>

    <main>
        <div class="container">
            <div class="folder-list">
                <% if (folders.length > 0) { %>
                    <h2>Folders:</h2>
                    <ul>
                        <% folders.forEach(folder => { %>
                            <li><a href="/?folderId=<%= folder.id %>"><%= folder.name %></a></li>
                        <% }); %>
                    </ul>
                <% } %>
            </div>

            <div class="file-list">
                <% if (files.length > 0) { %>
                    <h2>Files:</h2>
                    <ul>
                        <% files.forEach(file => { %>
                            <li><a href="https://drive.google.com/uc?id=<%= file.id %>&export=download" target="_blank"><%= file.name %></a></li>
                        <% }); %>
                    </ul>
                <% } %>
            </div>
        </div>
    </main>

    <footer>
        <p>Funded By - DST SERB</p>
    </footer>
</body>
</html>
